{
  "name": "rille33",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "build:einzeldatei": "vite build --mode einzeldatei && node scripts/einzeldatei.mjs"
  },
  "dependencies": {
    "three": "^0.186.0"
  },
  "devDependencies": {
    "vite": "^8.3.0",
    "vite-plugin-pwa": "^1.3.0"
  }
}
